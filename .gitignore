# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Active-learning candidate images are tracked on the `contributions` branch
# only (via the GitHub Contents API, not local git) — keep local/dev runs of
# api/main.py from accidentally staging images into a main checkout. The
# manifest itself IS tracked on main (audit trail), so only images/ is ignored.
data_pending/images/

# Scratch / runtime
_online/
result.jpg
*.log
X_Augumented_Grayscale
y_Augumented_Grayscale
logs/

# Secrets (Pl@ntNet API key etc.) — never commit
.streamlit/secrets.toml
.env

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Tests and tooling
.pytest_cache/
