# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Pending active-learning candidates are tracked in git on the `contributions`
# branch only — never on main locally during dev.

# Scratch / runtime
_online/
result.jpg
*.log
X_Augumented_Grayscale
y_Augumented_Grayscale
logs/

# Secrets (Pl@ntNet API key etc.) — never commit
.streamlit/secrets.toml
.env

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Tests and tooling
.pytest_cache/
